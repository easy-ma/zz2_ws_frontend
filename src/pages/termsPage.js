import React from "react";
import { Box, Center, Heading, List, ListItem, Text } from "@chakra-ui/react";

const TermsPage = () => {
  return (
    <Center>
      <Box>
        <Heading>No terms for now</Heading>
      </Box>
    </Center>
  );
};

export default TermsPage;
